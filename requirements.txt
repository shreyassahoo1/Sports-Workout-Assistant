# FORMScope — AI Workout Form Correction System
# Install with: pip install -r requirements.txt

# Core vision + pose estimation
mediapipe>=0.10.9
opencv-python>=4.9.0

# Text-to-speech (offline, cross-platform)
pyttsx3>=2.90

# Numerics (mediapipe dependency, listed explicitly)
numpy>=1.24.0

# Optional: for running video-file sources with audio stripped
# ffmpeg-python>=0.2.0
